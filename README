Requests to create table in bycontractbd

****** CREATE FOLDER TABLE *****

CREATE TABLE folder (
	id_folder VARCHAR(255) PRIMARY KEY,
	id_user INTEGER NOT NULL,
	id_parent_folder VARCHAR(255),
	folder_name VARCHAR(255) NOT NULL,
	created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	modified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	size INTEGER,
	CONSTRAINT fk_creator FOREIGN KEY (id_user) REFERENCES users(id_user) ON DELETE CASCADE,
	CONSTRAINT fk_parent_folder FOREIGN KEY (id_parent_folder) REFERENCES folder(id_folder) ON DELETE CASCADE
);


****** CREATE FILE TABLE *****


CREATE TABLE file (
	id_file VARCHAR(255) PRIMARY KEY,
	id_user INTEGER NOT NULL,
	id_parent_folder VARCHAR(255),
	file_name VARCHAR(255) NOT NULL,
	created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	modified_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	size INTEGER,
	CONSTRAINT fk_creator FOREIGN KEY (id_user) REFERENCES users(id_user) ON DELETE CASCADE,
	CONSTRAINT fk_parent_folder FOREIGN KEY (id_parent_folder) REFERENCES folder(id_folder) ON DELETE CASCADE
);

****** DELETE ALL DATA IN A TABLE  *****

DELETE FROM file;

****** DELETE ALL DATA IN A TABLE A RE-INITIALIZE THE ID *****

TRUNCATE TABLE users RESTART IDENTITY CASCADE;

****** EXTRACT A DATABASE FROM POSTGRESQL *****

pg_dump -U postgres(name of the owner) -b bycontractbd(database name) -f bycontractbd.sql(sql file name)

****** FOR THE TESTS *****
In pgAdmin, create a new database called bycontractbd_test

****** GENERATE A RANDOW IDENTIFIER *****

java.util.UUID.randomUUID().toString()

****** RUN THE PROJECT IN THE TERMINAL *****
./mwnw spring-boot:run


***********************************
ApplicationConfig, c'est le bureau de la sécurité à l'entrée du bâtiment. Il définit les règles : qui est le vigile, quel type de scanner de badge utiliser (le PasswordEncoder), et comment vérifier la liste des employés. Il n'y a qu'un seul bureau de sécurité pour tout le bâtiment.

CustomUserDetails, c'est le badge nominatif que l'on donne à un employé (ex: Adams) quand il rentre. Ce badge contient son nom, son ID, et ses droits d'accès (rôles). Il y a autant de badges que d'employés connectés.


************************************

-- Insertion pour le premier contact (Idemia)
INSERT INTO main_contact_companies (id_company, first_name, last_name, email, phone, created_at, modified_at)
VALUES (
    (SELECT id FROM companies WHERE name ILIKE '%Idemia%' LIMIT 1),
    'Luc', 'BRILLARD', 'lucbrillard@idemia.com', '0758651247', NOW(), NOW()
);

-- Insertion pour le deuxième contact (Dassault)
INSERT INTO main_contact_companies (id_company, first_name, last_name, email, phone, created_at, modified_at)
VALUES (
    (SELECT id FROM companies WHERE name ILIKE '%Dassault%' LIMIT 1),
    'Lyne', 'BOURDON', 'lynebourdon@dassault.com', '0698143298', NOW(), NOW()
);